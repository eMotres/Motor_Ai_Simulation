# Полный диск (n_sectors=1) vs сектор — точная диагностика (заменяет DEBUG_FINDINGS_sector_vs_full.md)

**Дата:** июнь 2026. Все эксперименты — в scratch-скриптах `_diag_*.py` (untracked), прод не изменён.

## Итог
- **Сектор корректен и валиден.** ns=4 gap|B|=1.316 Т, ns=2 gap|B|=1.313 Т — согласованы, масштабируются (tris ×1, ×2), момент 59.6 Н·м.
- **Сломан ТОЛЬКО ns=1** — единственный путь без `_clip_polys_to_sector` (стр. 1244, клип лишь при n_sectors>1).
- Старый `DEBUG_FINDINGS_sector_vs_full.md` НЕВЕРЕН: «трещина сетки на mid_r» — не причина (сварка применена, coincident_pairs=0, не помогло).

## Что ИСКЛЮЧЕНО прямой проверкой (НЕ причины)
| Гипотеза | Проверка | Результат |
|---|---|---|
| Намагниченность M | полярность по углу | `-+-+…` идеально в обоих |
| Граничные условия | внешнее ГУ Дирихле | 130 мм в обоих, дальнее поле есть |
| Связность сетки | connected_components | 1 компонента в обоих (разрыва нет) |
| Качество (slivers) | min-angle quality | ns=1 даже лучше: 1.2% slivers vs 3.5% |
| Вершины полигонов | счётчик | масштаб ×4 идеально (статор 369→1466) |
| Кольцо скольжения | _N_SLIP 1008→120 | gap|B| 0.377→0.413, tris ~140k (не помогло) |
| Слияние воздуха | legacy continuous-air | gap|B|=0.082 (хуже!) |
| Demag | demag OFF | ns=1 всё равно 0.377 vs 1.316 (demag — вторичен) |

## Подтверждённые дефекты ns=1
1. **Перемельчение сетки** (от gmsh size-полей/кривизны на full-360°, НЕ от вершин):
   tris=170848 (×12 вместо ×4). Статор 1754→65070 (×37), outer 5416→64544 (×12), airgap 4092→29578 (×7). Магниты/coils/ротор — ×4 верно.
2. **Потерян домен вала**: shaft 246→0 ячеек (ушёл в rotor: 1790→8214). Дефект классификации `_classify`/`frag_to_doms` на full-360°.
3. **Нелинейная итерация насыщения сходится к НЕВЕРНОЙ точке**: 1-я итер→сошлось: ns=4 gap|B| 0.081→1.316 (РАСТЁТ ✓), ns=1 0.494→0.377 (ПАДАЕТ ✗). На полном диске μ_r железа итеративно занижается → железо не проводит поток → поле гаснет ×3.5.

## Вывод для фикса
Корень — генерация full-360° сетки (`build_mesh_from_polygons` size-поля + классификация) + поведение нелинейной итерации на ней. Это **не однострочник**: правка затрагивает валидированный код, общий с рабочими ns=2/4. Нужна аккуратная пошаговая переделка с регрессионной проверкой ns=2/4 на каждом шаге.

**Безопасная альтернатива:** «Full» вид считать через ns=4 и размножать поле ×4 (анти-периодично) — 1/1 совпадёт с 1/4 по построению, физически корректно, нулевой риск для солвера.

---

## Углублённая диагностика (раунд 2) — механизм локализован, фикс не найден

**ГЛАВНОЕ:** поле в зазоре полного диска **НЕКОГЕРЕНТНО** (осциллирует ячейка-к-ячейке). B_r в зазоре, усреднённый по 1°-бинам: ns=4 binned-peak=2.53 (=пик ячейки 2.93, когерентно); ns=1 binned-peak=0.023 при пике ячейки 1.46, смен знака 106 (ждём 28). Когерентный поток разрушается осцилляцией → равномерно слабое поле.

**Воспроизводится в ПРОСТЕЙШЕМ случае:** μ_r=1 ВЕЗДЕ (без железа, только магниты в свободном пространстве, задача Пуассона) — ns=4 когерентность 0.89, ns=1 **0.08**. Это ОКОНЧАТЕЛЬНО исключает железо/насыщение/demag.

**Проверено и ОТБРОШЕНО как причина (раунд 2):**
- Линейное решение (μ=5000 фикс, без насыщения): ns=1 gap=0.494 vs ns=4 gap=0.068 — расходятся уже линейно (баг в линейной системе, не в итерации).
- Площадно-взвешенный перцентиль насыщения: ns=1 0.377→0.472 (мизер), ns=4 1.316→1.341 (ок). Density-зависимость — малый вклад.
- Скачок на слип-поверхности mid_r: НЕТ — внутренняя (0.042) и внешняя (0.002) половины зазора по отдельности обе некогерентны.
- gap_layers 1..5: почти без эффекта (0.186→0.098).
- Вал как ротор (μ 1→5000): сектор лишь 1.316→1.264 (4%). Не причина.
- Дубли треугольников: ns=1 имеет 3158 дублей, НО это вырожденные нулевой площади (6284 шт); удаление их НЕ меняет поле. Не причина.
- Связность сетки: 1 компонента. Дубли узлов: 0.

**Сухой остаток:** линейная система на полной 360° сетке даёт слабое некогерентное поле; клиппинг ns=2/4 — нет.

**Раунд 3 — source-vs-matrix split (выполнено):**
- RHS магнитов (источник) при μ=1 **ЧИСТ и масштабируется ×4** (ns=1: 1120 ненулевых узлов = 4×280, тот же max=3421). → баг НЕ в источнике.
- Вырожденные ячейки (6284 нулевой площади) **исключены**: их 0 в радиальном зазоре, исключение из измерения не меняет ни |B|, ни когерентность.
- Зазорная полоса [r_ro,r_si] у ns=1 = 80356 ячеек против 5761 у ns=4 — ×14 (а не ×4), т.е. зазор перемельчён ~×3.5: gmsh мельчит до ~0.06мм, хотя MathEval-формула велит 0.217мм (источник перемельчения так и не пойман: не кольца _N_SLIP, не curvature/points, не gap_layers).

**ВЫВОД:** источник чист → дефект в сетке.

## РАУНД 4 — КОРНЕВАЯ ПРИЧИНА НАЙДЕНА (окончательно)

**Невязка линейной системы:** ns=1 ‖KA−f‖/‖f‖ = 3.7e-15 (решается ТОЧНО). Значит солвер исправен, A — точное решение системы ns=1.

**Один магнит + железо (μ=5000):** ns=4 rotor|B|max=26 (поток ВХОДИТ в железо), ns=1 rotor|B|max=**1.2** (НЕ входит). Даже один магнит не передаёт поток в полюсный наконечник полного диска → не взаимодействие 28 магнитов, а фундаментальная связь магнит→железо.

**КОНФОРМНОСТЬ РЁБЕР (decisive):** число рёбер, принадлежащих >2 треугольникам:
- ns=4: **0** (валидная многообразная сетка) → gap|B|=1.316 ✓
- ns=1: **12279** → gap|B|=0.377 ✗

Ребро не может быть в >2 треугольниках — это **НАЛОЖЕННЫЕ (overlapping) треугольники**. **OCC boolean-fragment НЕ создаёт чистого (manifold) разбиения для полной 360° геометрии** — сетка местами покрывает область дважды (stacked triangles) → сборка K/источника идёт по наложенным элементам → магнитный поток не идёт правильным путём (магнит→полюс→зазор) → равномерно слабое поле. Сектор (ns=2/4) чист (0), т.к. `_clip_polys_to_sector` пере-резолвит геометрию через Shapely-intersection до простой, которую OCC корректно фрагментирует.

Также найдена конкретная площадная накладка: `out_band` ∩ `coils` = 1872 мм² (внешний воздух вычитает только статор, не катушки в пазах).

**Фиксы, проверенные и НЕДОСТАТОЧНЫЕ (overlap частичный, не лечится просто):**
- Пост-меш удаление точных дублей/нулевых: >2edges 12279→7082, поле 0.377 (без изменений).
- `shapely.set_precision` (привязка к сетке 1e-3/1e-2 мм): >2edges→10936, поле→0.453 (мизер).
- legacy воздух: >2edges=5968, поле 0.082 (хуже).
- Вычитание катушек из out_band: не решает (остальные наложения остаются).

## ИСТИННЫЙ ФИКС (требует переработки mesh-пайплайна для ns=1)
OCC-fragment ненадёжен на полной 360°. Варианты:
1. **Планарное разбиение через Shapely (рекомендуется):** объединить ГРАНИЦЫ всех полигонов (`unary_union` линий) → `polygonize` в непересекающиеся грани → назначить домен каждой грани (point-in-polygon) → меш. Гарантирует manifold-разбиение, не зависит от OCC booleans.
2. **`gmsh occ.removeAllDuplicates()` перед fragment + переклассификация фрагментов по центроиду/point-in-polygon** (сохранить per-magnet/coil теги). Менее надёжно, ломает текущую classification по out_map.
3. **Безопасный обход:** Full = решение ns=4, размноженное ×4 анти-периодично (1/1≡1/4 по построению, корректно, нулевой риск).

Любой фикс валидировать регрессией ns=2/4 (должны остаться 1.313/1.316). Прод НЕ изменён (все правки/инструментовка откатаны; ns=4=T_em −0.98/Bmax 76.6). Диагностика: `_diag_*.py`, `_plot_flux.py`, `_plot_zoom.py`.

**Прод НЕ изменён** (все правки откатаны; ns=4 = T_em −0.98 / Bmax 76.6). Диагностические скрипты: `_diag_*.py`, `_plot_flux.py`.

# Saved-state energy reconstruction checkpoint — 2026-09-23

The read-only script `scratchpad/torque_energy_reconstruction_20260923.py` verifies the **exact bytes** of the material library, saved scratch configuration and solver source against the run04 provenance before interpreting its data. SHA-256 hashes match: material library `80b269b6ab344458409fd7c56ee9e0e84c58fed0af4211f4af14f022d9b80136`, configuration `f86e751dd57ca0cfa8e9d8dac46620dc723d11e446a4ce962ea5d4be4c6caba8`, solver `528fbe27ca0688f5d1ddeeacd8129f8fa058c1a0805fc2b7984c90dc3132e805`. Thus a matching library snapshot is available. The four saved frames contain 17,299 P2 DOFs and quadrature B of shape `(4, 8107, 6)`; the maximum saved `|B|` is 3.83545 T. The data include magnet tags 100–106, but not the **assembled** material or magnet-source arrays associated with each element.

For isotropic reversible iron with the accepted solver law `H(B)`, stored magnetic energy density is `u_iron(|B|) = ∫₀^|B| H(b) db` in J/m³. `field_ops._mu_r_from_bh_vec` interpolates H linearly in B, uses an implicit origin for positive-first-B tables, extends above the final point with slope `1/μ₀`, and floors relative permeability at one. An exact integral must follow that same piecewise law and its floor where active. It cannot be replaced by `½ B·H(B)` or by `½ AᵀK(A)A` in nonlinear iron. The P2 Newton path applies that law at quadrature points through `p2_nonlinear.P2Nonlinear.Kpw`; element-mean B would give a different energy.

For the linear recoil PM model used in this solve, the field-dependent potential density whose derivative matches the weak form is `u_PM(B) = ½ν|B|² − H_c·B + constant`, with `H_c = M/μ_rec` as assembled in `fem_solver_2d.py` (`_Mx_glob`, `_My_glob`, `_mx_all`, `_my_all`, `f_mag2`). Its derivative is `νB − H_c`. The separate winding source `J_z A_z` belongs to the external-current potential, not to stored magnetic energy. A PM reference constant must be fixed consistently before interpreting absolute values; the present no-demag, fixed-material case could cancel a common constant between equal material volumes, but that cancellation has not been independently validated here.

The script checks the formulas on a small analytic, monotone nonlinear B-H table where the permeability floor is inactive, and on a linear PM vector. Central differences of energy density recover their expected `H` gradients with relative errors `6.50e-11` and `5.43e-12`. This checks the algebra only; it does not turn the saved machine fields into a validated energy balance.

At this first checkpoint, **run04 total stored energy was not evaluated** because its NPZ has neither the element-by-element `nu_base2` and `_sat2` assignment used by the accepted solve nor the effective per-element PM vectors `_mx_all/_my_all`. The matching library file and mesh tags identify possible source cards, but the actual source vectors depend on each magnet polygon's orientation and the per-element magnet scale; those assembled vectors were not captured in run04. The seven magnet tags also permute under the physical one-period rotor map. Rebuilding approximate assignments from the current config or mesh centroids would quietly substitute a different model. The assembly-only reconstruction below resolves the material-array gap without solving another FEM frame. Periodic stored energy remains separate from a validated instantaneous torque method, especially when losses or irreversible state are present.

Verification: `C:\Users\vadim\AppData\Local\Programs\Python\Python311\python.exe scratchpad/torque_energy_reconstruction_20260923.py` passed in under one second, without a FEM solve, file write, API call, or configuration change. Implemented by Codex Sol; no model escalation.

## Assembly interception attempt and correction

An attempted assembly-only capture in `scratchpad/torque_material_capture_20260923.py` **did not stop before solving**. Its first hook patched `fem_solver_2d._P2Nonlinear`, but the P2 branch imports `P2Nonlinear` locally from `p2_nonlinear` at `fem_solver_2d.py:4648`, bypassing that alias. The guarded invocation consequently completed one unintended 48×2 FEM run in the isolated `scratchpad/torque_material_20260923_run05` directory (about 58 s process CPU) and then failed its explicit “must intercept” assertion. It captured no assembled material arrays. The process exited; no live API, production source, or original configuration was changed. This was a failure of the capture plan and does not provide an energy result.

The corrected script patches the **local import target** `p2_nonlinear.P2Nonlinear` and installs hard stops on the original class constructor, direct solve entry and sparse factorization functions. A fake-module test confirmed interception and hard stops before the authorized assembly-only run described below.

## Corrected assembly-only reconstruction

After the failed run05 was recorded, one corrected `scratchpad/torque_material_capture_20260923.py` invocation was authorized with a 30-second wall cap. It intercepted the **locally imported** `p2_nonlinear.P2Nonlinear` constructor and raised `ControlledStop` before any FEM frame. The fresh `scratchpad/torque_material_20260923_run06` directory contains only the copied config, assembly capture, review and log; it has no raw waveform or solved-frame result. Mesh vertices, triangles, P2 element DOFs and quadrature weights are bit-identical to run04. The capture records exact `nu_base2`, all 3,051 saturable-element IDs with their B-H curves, effective PM source arrays `_mx_all/_my_all`, and `f_mag2` (SHA-256 for assembled arrays `ea00f2db382ade00e47e85cb271cef7ddad7e386b0f0495537d88cff8`; curves `e38f896e04be2722ce47adc9c847d26d98e202a56cba520cd0352cde2085a57d`). The saved-state check covered all 73,224 nonlinear quadrature values: reconstructed `H(B)` differs from production `_mu_r_from_bh_vec` by at most `2.33e-10 A/m`; no value lies at or below the production `B ≤ 1e-12 T` branch. Every curve knot from zero through the largest sampled B satisfies `H ≤ B/μ₀`; piecewise linearity makes that sufficient to rule out the `μ_r ≥ 1` floor anywhere along the integration path, not merely at sampled endpoints. The PM field integral equals `−A·f_mag2` after sector and stack scaling to 2.22e-16 J, checking the PM sign and quadrature pairing.

The resulting **whole-machine magnetic potential** uses nonlinear iron `∫H dB`, linear regions `½ν|B|²`, and PM term `−H_c·B`, integrated over two sectors and a 0.01 m stack. Its values at frames 0, 1, 48 and 49 are respectively `−0.35812778247707217`, `−0.35826986619387713`, `−0.3581266922692223`, and `−0.3582689801218128` J. The one-electrical-period differences are `+1.0902078498853385e-6` J (0→48) and `+8.860720643322217e-7` J (1→49). A PM reference constant can shift all four absolute values, so the **differences** are the interpretable result. They are 2.96 and 2.41 parts per million of the approximately 0.3676994 J hybrid torque × one-period angle scale, but this ratio is a scale comparison, not an uncertainty bound. Only two phase pairs have complete saved P2 states; near closure there does not certify the entire trajectory as periodic or independently validate torque. The reconstructed functional has the correct local derivative on the controlled iron/PM examples and the production PM-source pairing, but a full discrete virtual-work and port-energy balance still requires separate validation.

# Prepared 24s/28p sector torque probe — 2026-09-23

`scratchpad/torque_symmetry_probe_20260923.py` is prepared but **no motor solve has run**. Its default invocation performs synthetic checks only: the standard single-layer 24s/28p winding passes the actual sector guard at `NS=1,2,4`; a fake source confirms the first and only target is `θ=0°`, the imposed three-phase current is frozen and a second frame raises before solve; and an artificial postcheck failure confirms raw-array persistence. That preflight passed in 1.00 s with the installed Python 3.11 interpreter. The script also checks its in-memory source-insertion anchors. It does not import the test module or read the live API.

The fixture copies the explicit `G150` dimensions in `tests/test_masses.py`: 150 mm diameter, 35 mm stack, 24 slots, 28 poles, standard single-layer generated winding and the existing pinned B15AHV950M/F45SH_120C material assignments from the guarded motor runner. It keeps one fixed physical angle, `0°`, and one fixed source vector, `(42.4264068712,42.4264068712,−84.8528137424) A`, in all three isolated processes. It requests P2, a 4.0 mm nominal mesh with 0.3 mm minimum, structured gap, 48 nominal samples per electrical period (for slip-ring density), one thread, BelowNormal priority, no eddy/demag and no torque filter. The in-memory hook stops before frame `k=1`; the nominal 48×2 waveform is never executed. Each sector runs in a separate unique output directory under `scratchpad/torque_symmetry_20260923_run10/ns{1,2,4}` because the guarded runner's audit hook confines writes/chdir to one directory per process. The live configuration is copied inside each isolated directory and guarded against external writes.

For each sector the torque callback saves **raw full P2 A, quadrature B, mesh vertices/triangles, element DOFs, quadrature weights, ψ, imposed currents, unscaled sector torque, accepted residual/iteration status, boundary sign, slip spacing and snapped angle** in `single_frame_raw.npz` inside a `finally` block before any later checks. The review JSON records sector-scaled torque, mesh dimensions, source and solver hash. Runtime guards reject a second torque call, any nonzero shift, changed currents or changed sector. A reviewer should compare the actual mesh provenance, convergence and boundary signs before comparing scaled torques. Expected boundary signs are full `+1`, half `+1` (14 poles), quarter `−1` (7 poles). The full-ring builder may stitch a rotated half mesh, so NS1 and NS2 need not be independent mesh constructions.

The preflight command is `PYTHONPATH=src python scratchpad/torque_symmetry_probe_20260923.py` (PowerShell: set `$env:PYTHONPATH` to the repository's `src` before invoking the installed Python). A later authorized run would invoke `--run-sector 1`, `--run-sector 2`, then `--run-sector 4` in **separate processes**, each with an external hard wall cap and no retry after an actual solve failure. Sector 1 is likely the most expensive. No defensible measured cost exists yet for the 150 mm full-ring one-frame cold nonlinear solve, so the owner should choose the cap based on available time; a conservative proposed cap is 120 s per sector, with only one process active at a time. Inspect the first result before launching the next. Run all three on the same committed solver/material/config snapshot; hash and retain those files. The requested nominal mesh size does not guarantee identical element topology across sector modes, so agreement is a discrete **sector sensitivity** check, not proof of mesh-converged continuum torque. One angle cannot establish ripple, mean work over a period or harmonic equivalence. Those would need a separately approved common-angle grid and field mapping, without smoothing or physics filtering.

Prepared by Codex Sol, no escalation. Parent handles dataset/journal and any authorization to execute the motor solve.

## Authorized one-angle execution and limits

The first `run10/ns4` attempt stopped **before geometry, assembly or any FEM frame**: the synthetic preflight imported project configuration before the private output path was installed, so the guarded runner detected a workspace-root mismatch and exited in 1.2 s. The run-mode harness was corrected to use only standard-library/NumPy fake checks before isolation, and to assert that `motor_ai_sim.config` and `motor_ai_sim.workspace` were not already loaded. The separately executed package-level symmetry preflight remained passing. After this pre-solve failure, the parent authorized one corrected `run11` sequence. No live config or API was written.

The corrected `run11` processes each exited 0 and saved their raw P2 states before postchecks. Results at the same `θ=0°`, same `(42.4264068712,42.4264068712,−84.8528137424) A` and same **1680 slip nodes/full circumference** (`0.2142857143°` per cell) are:

| Sector | Boundary sign | Wall time | P2 DOFs / elements | Newton residual / iterations | Sector-scaled unfiltered torque |
|---|---:|---:|---:|---:|---:|
| Quarter, NS4 | −1 | 15.57 s | 28,661 / 13,734 | 3.6721e−8 / 14 | 40.9584901122 N·m |
| Half, NS2 | +1 | 24.22 s | 57,210 / 27,468 | 3.5624e−8 / 14 | 40.9523108569 N·m |
| Full, NS1 | +1 (no cut) | 43.22 s | 112,156 / 54,096 | 3.5111e−8 / 14 | 40.9426033576 N·m |

Relative to full, quarter is **+0.0158867546 N·m (+0.03880%)** and half is **+0.0097074994 N·m (+0.02371%)**. The saved phase linkages `(A,B,C)` are quarter `(0.025696510816,−0.013475513242,−0.015130351933) Wb`, half `(0.025694005177,−0.013464095690,−0.015132000564) Wb`, and full `(0.025690009670,−0.013458498290,−0.015130070707) Wb`. All four physical inputs and material names were frozen by the same runner source, all three private config copies have SHA-256 `f86e751dd57ca0cfa8e9d8dac46620dc723d11e446a4ce962ea5d4be4c6caba8`, the solver hash is `67b714f726e89a696014d25a59aa593aa41fffc70f04422b19629446548c12e7`, and the current material library hash is `80b269b6ab344458409fd7c56ee9e0e84c58fed0af4211f4af14f022d9b80136`. All three stderr logs report the requested 48 nominal samples snapping to **40** per period on the same 120-node electrical slip period; only the first frame was actually solved.

This was the **analytic/template geometry route** (`geo_mesh=False`), not a CAD-fidelity G150 mesh. Quarter and half element counts scale exactly by two; full has 54,096 elements rather than four times quarter's 54,936, consistent with a distinct full-ring meshing/stitching path. The raw NPZ files include full field and mesh topology but **not element material/coil tags or assembled constitutive arrays**. Identical request, config/library hash and guard-admissible winding therefore establish common intended material inputs, not byte-for-byte equivalence of the assembled material-region assignments. That limits the result to a coarse one-angle numerical sector comparison. It cannot certify field symmetry, ripple/mean torque, mesh convergence or exact equality of local topology. No additional angle or solve was run. Codex Sol; no model escalation.

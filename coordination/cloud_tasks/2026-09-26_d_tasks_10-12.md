# Облачные задания, пачка 4 (по одному на сессию)

Same rules as in `2026-09-26_b_tasks_4-6.md` (new branch → PR into `pre-migration-freeze-2026-09-15`, only real
physics, loud validation, one-line UI + HelpTip, run only touched tests, don't touch `config/dies/*`, avoid
`fem_solver_2d.py`). Eight PRs (#2–#9) are open and not merged yet — base your branch on
`pre-migration-freeze-2026-09-15` and keep your change away from the files they touch where you can.

---

## Задание 10 — переключатель «даташит / SPICE» в Controller

The inverter loss model already supports `switching_source: datasheet | spice` with SPICE switching tables stored
in the device cards (`src/motor_ai_sim/inverter/`, `config/devices/*.yaml` `switching_table`, docs
`docs/CONTROLLER_SPICE_2026-09-23.md`), but the web has no control for it. Add to the Controller tab: a select
"Switching losses: datasheet | SPICE" (SPICE enabled only for devices whose card has a trusted `switching_table`;
the others show why in the HelpTip), and inputs R_G on / R_G off (Ω), V_GS on/off (V), power-loop L (nH) used by
the SPICE table lookup (defaults = the datasheet test circuit, stated in the HelpTip). Saved with the controller
settings (auto-save exists). Default stays "datasheet". Results card states which source was used. Tests.

## Задание 11 — переименовать «honest»-ключи

The frequency-domain cross-check keys `P_mag_honest_W` / `P_shaft_honest_W` are now a LINEAR estimate
(`shaft_is_linear_estimate`, see `docs/EDDY_TIME_INTEGRATION_2026-09-25.md`). Rename them to
`P_mag_linear_W` / `P_shaft_linear_W` everywhere (solver result, records, web, report, datasheet), keeping the
old keys readable from stored records (read-compat only, never written again). Labels: "linear cross-check".
Tests for both the new keys and reading old records.

## Задание 12 — чистка мелкого текста в панелях оптимизации

Owner UI rule: one short line per control + HelpTip; no paragraphs of small grey text. Audit and clean
`web/src/components/sweep/DescentPanel.tsx` (~20 caption/secondary lines), `AutoOptimizePanel.tsx` (~16) and
`SweepStudyPanel.tsx`: move explanations into HelpTips, delete duplicates of labels, keep every validation
message, warning, stale/verify notice, units and anything the report reads. Write the list (file:line, text,
verdict remove / to HelpTip / keep) into the PR description. Web type-check touched files; node tests if they
exist.

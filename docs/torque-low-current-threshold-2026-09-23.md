# Low-current torque selector checkpoint, 23 September 2026

The saved GEO30, 12-slot/14-pole, NS2 cases use the same 1.4/0.35 mm spatial mesh, 144 slip cells per electrical period, 48 endpoint-excluded angular samples, and 15,000 rpm imposed synchronous current. Eddy, demagnetization, incremental permeability, and torque filtering were off. All four cases completed with converged fields. Case 4 (2 A peak per branch) has not run.

| Peak current per branch (A) | Raw Maxwell mean (Nm) | Space-vector candidate (Nm) | Spectral terminal-work candidate (Nm) | Selected mean (Nm) |
|---:|---:|---:|---:|---:|
| 0 | 0.000106686089 | 0 | 0 | 0.000106686089 |
| 0.5 | 0.002666779444 | 0.002570250688 | 0.002570250688 | 0.002666779444 |
| 1.0 | 0.005226917746 | 0.005140465326 | 0.005140465326 | 0.005226917746 |
| 1.001 | 0.005232037675 | 0.005145605691 | 0.005145605691 | 0.005145605691 |

The 1.000 to 1.001 A selected mean falls by **0.000081312055 Nm**, while the space-vector candidate rises by **0.000005140365 Nm** and raw Maxwell rises by **0.000005119930 Nm**. This is a selector discontinuity, not evidence that either candidate is the physical mean. The zero-current raw Maxwell mean of 0.000106686089 Nm is a possible mesh/stress bias; these data do not isolate physical cogging.

The independent offline [review script](../scripts/torque_low_current_review.py) computes mean terminal work using all DFT bins of raw current and corrected reciprocal flux linkage, with frequency in **mechanical radians** and no extra division by pole pairs. For these nearly fundamental waveforms it agrees with the space-vector candidate to numerical precision. Its validity depends on periodic continuation of the sampled terminal state; the cases did not capture endpoint magnetic energy or low-current virtual work. A 48-point spectral result alone cannot approve replacement of the selector, and zero-current terminal work is necessarily zero even if cogging exists. [Analytic tests](../tests/test_torque_low_current_review.py) verify fundamental plus fifth-harmonic work, signed angle reversal, current sign, and parallel scaling.

Provenance: cases 0–2 retained unchanged source hashes. Case 3's pre-run source hashes match cases 0–2 and its raw outputs and convergence checks were saved. An unrelated agent changed `sb_postproc.py` on disk during that run, after the process imported the prior version; the parent's diff audit found snapshot/retention metadata changes, not torque-method changes. Its end-of-run source-hash postcheck therefore failed. Case 3 was retained and was **not rerun**. This is a qualified provenance finding, not a claim that the disk source remained unchanged.

Run18 supplied that bounded discriminator at **1 A peak per branch**. It saved 48 center-current pairs, each solved at ±one slip cell on the same GEO30 NS2 mesh (96 converged equilibria). The read-only [virtual-work analyzer](../scripts/torque_frozen_lowcurrent_review.py) preserves all 48 secants and reconstructs the nonlinear P2 magnetic potential from `∫H dB`, linear `½νB²`, and PM `−Hc·B`, then forms winding-source coenergy at fixed branch current. The mean secant is **0.005138892113 N·m**, versus all-bin terminal work **0.005140465326 N·m** (difference −0.000001573213 N·m) and raw Maxwell **0.005226917746 N·m** (difference −0.000088025633 N·m). The secants range from 0.000432939702 to 0.010062487186 N·m, peak-to-peak 0.009629547484 N·m. Source–linkage mismatch is at most 1.63e−19 Wb; PM field/source pairing is within 3.33e−16 J; reconstructed H agrees with the production law to 4.66e−10 A/m, with no sampled or integration-knot permeability-floor activation.

Run18 pinned its exact `field_ops.py` Git blob from `d785a4c` and LF-normalized content. Run15's archived `field_ops.py` SHA-256 was captured from a mixed-EOL working tree whose bytes were not saved, so byte identity with that archive cannot be proven. Run18 checked source hashes before/after, the saved mesh, source vectors, B-H assignments, currents, angles, modes, and convergence. The finite ±one-cell secants still have unbounded integer-weld/spatial error; this is a diagnostic comparison, not a continuous-torque certification. [Two focused tests](../tests/test_torque_frozen_lowcurrent_review.py) cover signed pair arithmetic and branch-current source scaling.

The hard **1 A threshold is disproven as a physical eligibility rule**: it creates the measured selected-mean jump while the candidates and independent 1 A secants vary smoothly in mean. Production now uses the all-bin terminal-work mean only for a converged, conservative imposed-current run over uniform signed samples spanning an integer number of electrical periods. Eddy, rotor-eddy, voltage/PWM, demagnetisation, frozen-permeability, fractional/nonuniform and unconverged cases return raw Maxwell exactly. The complete Maxwell AC waveform is retained in every case; there is no torque filtering or sample rejection.

At zero current the selected full-period terminal-work mean is zero, but that does not erase cogging: the regenerated GEO30 baseline retains an absolute raw peak-to-peak span of **5.3396466e-5 N·m** and the separate raw Maxwell mean of **4.1481999e-5 N·m**. Relative ripple is reported as unavailable (`None`) rather than the false `0%`, because its denominator is zero. Optimisation treats an unavailable percentage as ineligible instead of a favourable zero. Other symmetry/current cases and a quantitative integer-weld/spatial error bound remain open validation work.

Verification: `python -m pytest tests/test_torque_low_current_review.py --noconftest -o addopts= -p no:cacheprovider -q` passed (5 tests), and the Run18 analyzer's 2 focused tests passed. The read-only commands are `python scripts/torque_low_current_review.py` and `python scripts/torque_frozen_lowcurrent_review.py`.

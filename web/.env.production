# Production API base URL — set this to your deployed FEM backend (Cloud Run) URL
# once the backend is deployed. Until then the hosted site loads but API features
# (geometry build, FEM, catalog Load) stay in "Local Mode".
VITE_API_URL=https://YOUR-CLOUD-RUN-BACKEND-URL

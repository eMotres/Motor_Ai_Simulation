# Complementary odd-slip period samples — 2026-09-23

One authorized, isolated quarter-sector P2 invocation solved only the **60 missing odd slip shifts** `1,3,…,119` on the same 1680-node/full-circumference ring as run13. The previous even shifts `0,2,…,118` were read, not rerun. The merged array has every integer slip shift `0…119` exactly once, at actual mechanical angles `shift·0.214285714286°`, endpoint excluded before `360/14=25.714285714286°`. The same explicit `Ipk=60√2 A`, electrical phase `14θ+60°` source drove both runs. The run14 script merges by recorded shift and verifies all 120 physical angles rather than assuming output-list positions.

| Grid | Mean torque | Sampled minimum | Sampled maximum | Sampled peak-to-peak |
|---|---:|---:|---:|---:|
| run13 even, 60 points | 40.2447851301 N·m | 37.3785102055 N·m at 11.57142857° | 42.9550310296 N·m at 0.85714286° | 5.5765208241 N·m |
| run13 + run14, 120 points | **40.2474570472 N·m** | **37.3785102055 N·m at 11.57142857°** | **42.9550310296 N·m at 0.85714286°** | **5.5765208241 N·m** |

Adding the odd cells changes the sampled mean by **+0.0026719171 N·m (+0.0066387% of the 120-point mean)**. It does not change the sampled extrema because both lie at even shifts. The odd-only 60-point mean is `40.2501289643 N·m`, showing the opposing offset of the complementary grid. This is angular sampling sensitivity at the **minimum available integer slip-cell step**, not a continuum or mesh-convergence proof. The 30-point nested grid's missed trough documented in `docs/torque-period-grid-2026-09-23.md` remains a concrete aliasing example; 60 and 120 happen to sample the same extrema on this mesh.

The run14 child process exited **0 in 95.91 s** under its 180 s cap, one thread at BelowNormal priority, solving exactly 60 frames and stopping before frame 61. Every Newton state was accepted; the maximum residual was `9.9543e−8`. Each frame saved unfiltered torque, three-phase current/ψ and convergence data plus an emergency full P2 `A` before postchecks. Frames 0,1,30,59 additionally saved full quadrature B and mesh/material provenance. All 60 scalar and all 60 emergency files are present under `scratchpad/torque_odd_slip_20260923_run14/ns4`; `odd_grid_review.json` contains the odd series and merged 120-point series. The actual P2 mesh vertices/triangles, DOFs, quadrature, element tags, base reluctivity, PM Hc arrays and nonlinear B-H assignments were checked **exactly** against run13; the config, solver and material-library SHA-256 values also matched before the solve. At shared odd shifts 3 and 9, run14 differs from the earlier run12 by at most `1.62136e−6 N·m` in torque and `9.9191e−10 Wb` in flux linkage. All compared states met their nonlinear residual criteria; those criteria alone do not bound torque or flux error.

The result pertains only to the 24s/28p **quarter-sector template geometry** (`geo_mesh=False`) and the prescribed synchronous current; no half/full-sector period run, CAD-fidelity comparison, endpoint field closure or stored-energy periodicity check was performed. `scratchpad/torque_odd_slip_probe_20260923.py` passed its fake-only angle/current/order, stop-before-61, isolation and forced-save-failure preflight before the single execution. No retries or other solves followed. Codex Sol; no model escalation. Parent handles dataset/journal records.

# Облачные задания, пачка 3 (по одному на сессию)

Same rules as in `2026-09-26_b_tasks_4-6.md` (new branch → PR into `pre-migration-freeze-2026-09-15`, only real
physics, loud validation, one-line UI + HelpTip, run only touched tests, don't touch `config/dies/*`, avoid
`fem_solver_2d.py`).

---

## Задание 7 — Robotics: один выбор пути отвода тепла

Thermal tab, Outer surface = Robotics. Owner: «давай упростим». Replace the current mount fields ("mount to arm,
W/K", "Mount into: ideal heat sink | robot link", link size preset, link material — commit 0513f73) by ONE
select "Heat path":
1. stator → housing (stator OD in contact with the housing; the housing sheds heat by still-air convection +
   radiation),
2. through the shaft (rotor → shaft → bearings → structure),
3. housing AND shaft together,
4. no contact — still air + radiation everywhere.
Internal defaults (housing size from the stator OD/length, contact conductances, shaft path) stated in the
HelpTip, no extra fields. Keep a 70 °C touch limit on the housing/structure node, judged beside winding/magnet
in steady, limits and S1; the limiting part named ("housing (touch 70 °C)"). Old saved settings (mount W/K,
link presets) still load and map to the nearest option. Code: `src/motor_ai_sim/routes/thermal.py`,
`simulation/cooling_models.py`, `thermal_settings.py`, `coupled_time_to_limit.py`, `coupled_continuous_rating.py`,
web `ThermalPanel.tsx`, `thermalStore.ts`, `roboticsHelp.ts`. Tests: each option changes the heat paths as
described; option 4 has no conduction path; old settings migrate.

## Задание 8 — 72 шага по умолчанию для расчётов с вихревыми токами

Owner decision: eddy-on runs (Simulation tab, coupled loop, report runs) default to 72 steps per electrical
period instead of 36 (BDF2 still reads the L155 magnets −4.3 % at 36, −1 % at 72; see
`docs/EDDY_TIME_INTEGRATION_2026-09-25.md`). The steps selector must stay visible and editable everywhere —
never forced (owner: «должен быть всегда выбор»). Optimizer screening keeps its own purpose-based sampling.
Where the ring/snap rules change the requested count, say so in one line. Tests for the defaults and that a
user-chosen value is always honoured.

## Задание 9 — посадочные места в карточках приборов

Device cards `config/devices/*.yaml` (Controller module). Add a `footprint` block: package outline ID (e.g.
PG-HDSOP-22-U01/U03, PG-WHSON-8-1, PG-TSON-8-4), land-pattern reference (datasheet figure/page), body height,
top-side cooling tab size, and a `compatibility_group` (e.g. `qdpak_750_1200` — same land pattern, different top
tab; `whson8_tson8` — same pads, different height). Values only from the official Infineon datasheets (cite
figure/page; mark anything not published as null with a note). In the Controller catalog, add a filter "fits
the selected board" by compatibility group, and a one-line warning when parts in one group differ in height or
top tab. Tests for the card schema and the filter.
